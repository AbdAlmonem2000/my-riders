SUPABASE_PROJECT_ID=
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_URL=
VITE_SUPABASE_PROJECT_ID=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_URL=

# Server-only, never exposed to the client bundle. Used by
# src/integrations/supabase/client.server.ts to bypass RLS for trusted
# server-side operations. Set this in your hosting provider's environment
# variables (Lovable/Vercel/etc.) — do NOT put the real value in this file
# or in .env.
SUPABASE_SERVICE_ROLE_KEY=
